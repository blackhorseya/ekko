apiVersion: v1
kind: Service
metadata:
  name: {{ include "ekko-restful.fullname" . }}
  labels:
    {{- include "ekko-restful.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ekko-restful.selectorLabels" . | nindent 4 }}
