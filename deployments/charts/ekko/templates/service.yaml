apiVersion: v1
kind: Service
metadata:
  name: {{ include "ekko.fullname" . }}
  labels:
    {{- include "ekko.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ekko.selectorLabels" . | nindent 4 }}
